name: Publish

on:
  push:
    tags:
      - '**'


jobs:
  dart_openapi_generator:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      environment: pub.dev
