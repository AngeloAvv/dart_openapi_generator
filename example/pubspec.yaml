name: dart_openapi_generator_example
description: Example project for dart_openapi_generator.
publish_to: none

resolution: workspace

environment:
  sdk: "^3.6.0"

dependencies:
  dart_openapi_generator_annotations:
    path: ../packages/dart_openapi_generator_annotations
  dio: "^5.9.2"

dev_dependencies:
  build_runner: "^2.15.0"
  dart_openapi_generator:
    path: ../packages/dart_openapi_generator
  lints: "^6.1.0"
